document.addEventListener('DOMContentLoaded', () => {
    const contentSection = document.getElementById('content');
    contentSection.innerHTML += `
        <p>JavaScript is working! You can add more interactive features here.</p>
    `;
});
